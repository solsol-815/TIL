import java.util.*;

public class Solution {
    public int solution(int n) {
        int answer = 0;
        String[] string = Integer.toString(n).split("");
        for(String num : string) {
            answer += Integer.parseInt(num);
        }
        return answer;
    }
}