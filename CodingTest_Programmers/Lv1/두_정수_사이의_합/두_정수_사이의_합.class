class Solution {
    public long solution(int a, int b) {
        long answer = 0;
        int min = Math.min(a, b);
        int max = Math.max(a, b);
        long count = max - min + 1;
        long sum = (max + min) * count / 2;
        return answer = sum;
    }
}